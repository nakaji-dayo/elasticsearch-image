machine:
  services:
    - docker

dependencies:
  post:
    - mkdir ./target
    - ./compile

deployment:
  master:
    branch: master
    commands:
      - go get github.com/aktau/github-release
      - cp $CIRCLE_ARTIFACTS/*.rpm .
      - ./github-release.sh
